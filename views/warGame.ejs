<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>War Game</title>
    <link rel="stylesheet" href="/index.css">
</head>
<body>
    <div id="game">
        <% if(cards.length < 1) { %>
            <a href="/randomhand" onclick="cardMove()">
                <img src="/images/backCard.png" alt="">
            </a>
            <h2>You don't have any cards yet.</h2>
        <% } else if ((cards.length % 2) == 1) { %>
            <a href="/randomhand" onclick="cardMove()">
                <img src="/images/backCard.png" alt="">
            </a>
            <div id="main">
                <div>
                    <h1>Player 1:</h1>
                    <img src="<%= cards[cards.length - 1][0].image %>" alt="not working">
                </div>
            </div>
        <% } else if ((cards.length % 2) == 0) { %>
            <a href="/randomhand" onclick="cardMove()">
                <img src="/images/backCard.png" alt="">
            </a>
            <div id="main">
                <div>
                    <h1>Player 1:</h1>
                    <img src="<%= cards[cards.length - 2][0].image %>" alt="not working">
                </div>
    
                <div>
                    <h1>Player 2:</h1>
                    <img src="<%= cards[cards.length - 1][0].image %>" alt="not working">
                </div>
            </div>

            <% if (Number(cards[cards.length - 2][0].value) > Number(cards[cards.length - 1][0].value)) { %>
                <h1>Player 1 Wins</h1>
            <% } else if (Number(cards[cards.length - 2][0].value) < Number(cards[cards.length - 1][0].value)) { %>
                <h1>Player 2 Wins</h1>
            <% } else { %>
                <h1>Tie or something wrong</h1>
            <% } %>
        <% } %>
    </div>
    <script src="/js/warGame.js"></script>
</body>
</html>