<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blackjack</title>
</head>
<body>
    <h1>This is the game YAY</h1><br><br>
    <div class="player-game-stuff">
        <form action="/randomhand">
            <label>Player:</label><br>
            <input type="submit", value="Hit Me">
        </form>
        <% if(cards.length < 1) { %>
            <h2>You don't have any cards yet.</h2>
        <% } else { %>
            <h3><%= number %> %></h3>
            <img src="<%= cards[cards.length-1][0].image %>" alt="<%= cards[cards.length-1][0].image %>">
        <% } %>
    </div>
    <div class="dealer-game-stuff">
        
    </div>
</body>
</html>